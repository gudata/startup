<h1>Списък квартали</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @quarters.each do |quarter| %>
    <tr>
      <td><%= quarter.name %></td>
      <td><%= link_to 'Show', [:admin, @town, @district, quarter] %></td>
      <td><%= link_to 'Edit', edit_admin_town_district_quarter_path(@town, @district, quarter) %></td>
      <td><%= link_to 'Destroy', [:admin, @town, @district, quarter], :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<br />

<%= link_to 'New Quarter', new_admin_town_district_quarter_path(@town, @district, quarter) %>
