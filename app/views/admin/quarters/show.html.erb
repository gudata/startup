<p id="notice"><%= notice %></p>

<table>

  <tr>
    <td>Квартали: <% @district.quarters.each do |quarter| %>
        <p><%= quarter.name %></p>
      <% end %></td>
  </tr>

</table>

<br />

<%= link_to 'Back', admin_town_district_quarters_path(@town, @district) %>
