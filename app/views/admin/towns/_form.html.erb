<%= semantic_form_for([:admin, @town]) do |f| %>
  <% if @town.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@town.errors.count, "error") %> prohibited this town from being saved:</h2>

      <ul>
        <% @town.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <% f.inputs :name => "Основна информация"  do %>
    <%= f.input :name %>
    <%= f.input :main_city, :as => :boolean %>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
