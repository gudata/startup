<h1>Списък райони</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @districts.each do |district| %>
    <tr>
      <td><%= link_to 'Show', [:admin, district] %></td>
      <td><%= link_to 'Edit', edit_admin_town_district_path(district) %></td>
      <td><%= link_to 'Destroy', [:admin, district], :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<br />

<%= link_to 'New District', new_admin_district_path %>