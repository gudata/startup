<h1>Списък страници</h1>

<table>
  <tr>
    <th>Заглвие</th>
    <th>Текст</th>
    <th>Публикувано на</th>
    <th>Автор</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admin_pages.each do |admin_page| %>
  <tr>
    <td><%= admin_page.title %></td>
    <td><%= admin_page.body %></td>
    <td><%= admin_page.published_at %></td>
    <td><%= admin_page.author %></td>
    <td><%= link_to 'Show', admin_page %></td>
    <td><%= link_to 'Редактирай', edit_admin_page_path(admin_page) %></td>
    <td><%= link_to 'Изтрий', admin_page, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Нова страница', new_admin_page_path %>
